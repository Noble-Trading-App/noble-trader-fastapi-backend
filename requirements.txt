fastapi>=0.111.0
uvicorn[standard]>=0.29.0
hmmlearn>=0.3.2
numpy>=1.26.0
scipy>=1.13.0
pandas>=2.2.0
pydantic>=2.7.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
redis>=5.0.0
PyJWT[crypto]>=2.12.1
httpx>=0.27.0
clerk-sdk-python>=0.1.0
# v3.0 optional GPU + feed adapter dependencies
# GPU HMM (install for 10-50x speedup on large datasets)
# pomegranate>=1.0.0
# torch>=2.0.0
# Feed adapters (install only the adapter you need)
# alpaca-py>=0.20.0        # Alpaca Markets
# websockets>=12.0          # Binance WebSocket
# ib_async>=0.9.0           # Interactive Brokers
